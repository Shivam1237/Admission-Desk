import { LeadForm } from "@/components/lead-form";
import { PageHeader } from "@/components/ui";
export default function NewLeadPage() { return <><PageHeader eyebrow="Pipeline" title="Add a lead" subtitle="Capture the first contact with enough context for the next follow-up." /><LeadForm /></>; }
